= link_to 'Back To List', file_reports_path, class: 'btn btn-primary'
%h3
  = 'List of Books under File Report' + File.basename(@file_report.data_file.to_s)
  .media
    .media-body
      %table.table.table-hover
        %thead
          %tr
            %th Title
            %th Author
            %th Date Published
            %th Unique Code
            %th Publisher
          - @file_report.books.each do |book|
            %tr
              %td= book.title
              %td= book.author
              %td= book.date_published
              %td= book.unique_code
              %td= book.publisher